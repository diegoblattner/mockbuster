{
  "name": "mockbuster",
  "private": true,
  "version": "1.0.0",
  "description": "a blockbuster mock app",
  "license": "ISC",
  "author": "diblattner@gmail.com",
  "scripts": {
    "dev": "rm -r packages/webapp/node_modules/.vite && npm run dev -w packages/webapp",
    "dev:client": "npm run dev -w packages/webapp",
    "dev:server": "npm run dev -w packages/server",
    "build": "echo \"Error: no build specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1",
    "storybook": "npm run storybook -w packages/ui-lib"
  },
  "workspaces": [
    "packages/shared",
    "packages/ui-lib",
    "packages/webapp",
    "packages/server"
  ]
}
